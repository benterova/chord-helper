import React from 'react';

export const Header: React.FC = () => {
    return (
        <header style={{ marginBottom: '2rem' }}>
            <h1 style={{ marginBottom: '1rem' }}>Chord & Scale Helper</h1>
        </header>
    );
};
